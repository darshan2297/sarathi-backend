# --- Phase 0 (data) needs only the standard library ---
# The dependencies below are for later phases (kept here so the env is ready).

# Web / async API (Phase 1+)
fastapi>=0.111
uvicorn[standard]>=0.30
websockets>=12.0

# AI orchestration (Phase 3+)
langgraph>=0.2
langchain>=0.2
langchain-community>=0.2

# LLM providers (Phase 2+)
openai>=1.30          # OpenRouter is OpenAI-API-compatible
httpx>=0.27
ollama>=0.3

# Data / storage (Phase 1/5+)
motor>=3.4            # async MongoDB
pydantic>=2.7
pydantic-settings>=2.3

# Observability (Phase 7)
structlog>=24.1

# Dev / test
pytest>=8.2
